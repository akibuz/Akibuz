<!DOCTYPE html>
<html lang="eu">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Akibuz Ceramics</title>
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Ink+Free&family=Lato:wght@300;400;700&display=swap" rel="stylesheet" />
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

    :root {
      --clay: #6b7c3f;
      --clay-light: #e8edda;
      --clay-dark: #445228;
      --earth: #2b3020;
      --cream: #f5f7f0;
      --sage: #7a8c6e;
      --ink: 'Ink Free', cursive;
      --body: 'Lato', sans-serif;
    }

    html { scroll-behavior: smooth; }

    body {
      font-family: var(--body);
      background: var(--cream);
      color: var(--earth);
      line-height: 1.7;
    }

    /* NAV */
    nav {
      position: fixed;
      top: 0; left: 0; right: 0;
      z-index: 100;
      background: rgba(250, 246, 242, 0.95);
      backdrop-filter: blur(8px);
      border-bottom: 1px solid rgba(107,124,63,0.15);
      padding: 0 2rem;
      height: 70px;
      display: flex;
      align-items: center;
      justify-content: space-between;
    }

    .nav-logo {
      display: flex;
      align-items: center;
      gap: 0.75rem;
      text-decoration: none;
    }

    /* Logo placeholder — reemplázalo con <img src="tu-logo.png" /> */
    .logo-img {
      width: 44px;
      height: 44px;
      border-radius: 50%;
      background: var(--clay);
      display: flex;
      align-items: center;
      justify-content: center;
      overflow: hidden;
    }

    .logo-img img {
      width: 100%;
      height: 100%;
      object-fit: cover;
      border-radius: 50%;
    }

    .logo-placeholder-text {
      font-family: var(--ink);
      font-size: 18px;
      color: #fff;
    }

    .brand-name {
      font-family: var(--ink);
      font-size: 1.5rem;
      color: var(--clay-dark);
      letter-spacing: 0.02em;
    }

    .nav-links {
      display: flex;
      align-items: center;
      gap: 2rem;
      list-style: none;
    }

    .nav-links a {
      font-family: var(--body);
      font-size: 0.9rem;
      font-weight: 400;
      letter-spacing: 0.05em;
      text-transform: uppercase;
      text-decoration: none;
      color: var(--earth);
      transition: color 0.2s;
    }

    .nav-links a:hover { color: var(--clay); }

    .nav-links .btn-instagram {
      display: flex;
      align-items: center;
      justify-content: center;
      width: 34px;
      height: 34px;
      border-radius: 50%;
      border: 1.5px solid var(--clay);
      color: var(--clay-dark);
      transition: background 0.2s, color 0.2s, transform 0.15s;
      padding: 0;
    }

    .nav-links .btn-instagram:hover {
      background: var(--clay);
      color: #fff;
      transform: translateY(-1px);
    }

    .nav-links .btn-etsy {
      background: var(--clay);
      color: #fff !important;
      padding: 0.45rem 1.1rem;
      border-radius: 50px;
      font-weight: 700;
      transition: background 0.2s, transform 0.15s;
    }

    .nav-links .btn-etsy:hover {
      background: var(--clay-dark);
      transform: translateY(-1px);
    }

    /* HERO */
    .hero {
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      flex-direction: column;
      text-align: center;
      padding: 8rem 2rem 4rem;
      background: linear-gradient(rgba(43,48,32,0.55), rgba(43,48,32,0.55)), url('1000029529.jpg') center center / cover no-repeat;
    }

    .hero-tag {
      font-family: var(--ink);
      font-size: 1rem;
      color: #d6e8c0;
      letter-spacing: 0.15em;
      margin-bottom: 1rem;
      opacity: 0;
      animation: fadeUp 0.8s ease forwards 0.2s;
    }

    .hero h1 {
      font-family: var(--ink);
      font-size: clamp(3rem, 8vw, 6rem);
      color: #ffffff;
      margin-bottom: 1.25rem;
      opacity: 0;
      animation: fadeUp 0.8s ease forwards 0.4s;
    }

    .hero p {
      font-size: 1.1rem;
      color: rgba(255,255,255,0.85);
      max-width: 440px;
      margin: 0 auto 2.5rem;
      font-weight: 300;
      opacity: 0;
      animation: fadeUp 0.8s ease forwards 0.6s;
    }

    .hero-ctas {
      display: flex;
      gap: 1rem;
      flex-wrap: wrap;
      justify-content: center;
      opacity: 0;
      animation: fadeUp 0.8s ease forwards 0.8s;
    }

    .btn-primary {
      background: var(--clay);
      color: #fff;
      border: none;
      padding: 0.75rem 2rem;
      border-radius: 50px;
      font-family: var(--body);
      font-size: 0.95rem;
      font-weight: 700;
      letter-spacing: 0.04em;
      cursor: pointer;
      text-decoration: none;
      transition: background 0.2s, transform 0.15s;
    }

    .btn-primary:hover { background: var(--clay-dark); transform: translateY(-2px); }

    .btn-outline {
      background: transparent;
      color: var(--clay-dark);
      border: 1.5px solid var(--clay);
      padding: 0.75rem 2rem;
      border-radius: 50px;
      font-family: var(--body);
      font-size: 0.95rem;
      font-weight: 700;
      letter-spacing: 0.04em;
      cursor: pointer;
      text-decoration: none;
      transition: all 0.2s;
    }

    .btn-outline:hover { background: var(--clay-light); transform: translateY(-2px); }

    /* SCROLL INDICATOR */
    .scroll-down {
      margin-top: 4rem;
      opacity: 0;
      animation: fadeUp 0.8s ease forwards 1.2s;
    }

    .scroll-down span {
      display: block;
      width: 24px;
      height: 24px;
      border-right: 2px solid rgba(255,255,255,0.7);
      border-bottom: 2px solid rgba(255,255,255,0.7);
      transform: rotate(45deg);
      margin: 0 auto;
      animation: bounce 1.4s ease infinite;
    }

    @keyframes bounce {
      0%, 100% { transform: rotate(45deg) translateY(0); }
      50% { transform: rotate(45deg) translateY(6px); }
    }

    /* SECTIONS */
    section { padding: 5rem 2rem; }

    .section-label {
      font-family: var(--ink);
      font-size: 0.95rem;
      color: var(--clay);
      letter-spacing: 0.15em;
      text-transform: uppercase;
      margin-bottom: 0.5rem;
    }

    .section-title {
      font-family: var(--ink);
      font-size: clamp(1.8rem, 4vw, 2.8rem);
      color: var(--clay-dark);
      margin-bottom: 1rem;
    }

    .section-sub {
      font-size: 1rem;
      color: #5a6a50;
      font-weight: 300;
      max-width: 500px;
    }

    /* CATALOG */
    #produktuak {
      background: #fff;
    }

    .catalog-header {
      text-align: center;
      margin-bottom: 3rem;
    }

    .catalog-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
      gap: 2rem;
      max-width: 1000px;
      margin: 0 auto;
    }

    .product-card {
      background: var(--cream);
      border-radius: 16px;
      overflow: hidden;
      transition: transform 0.3s, box-shadow 0.3s;
    }

    .product-card:hover {
      transform: translateY(-6px);
      box-shadow: 0 16px 40px rgba(59,46,37,0.12);
    }

    .product-card img {
      width: 100%;
      aspect-ratio: 1;
      object-fit: cover;
      display: block;
    }

    .product-card .card-body {
      padding: 1.25rem 1.5rem;
    }

    .product-card h3 {
      font-family: var(--ink);
      font-size: 1.3rem;
      color: var(--clay-dark);
      margin-bottom: 0.35rem;
    }

    .product-card p {
      font-size: 0.9rem;
      color: #5a6a50;
      font-weight: 300;
    }

    /* ETSY BANNER */
    .etsy-banner {
      background: var(--clay);
      text-align: center;
      padding: 4rem 2rem;
    }

    .etsy-banner p {
      font-family: var(--ink);
      font-size: clamp(1.4rem, 3vw, 2rem);
      color: #fff;
      margin-bottom: 1.5rem;
    }

    .btn-etsy-big {
      display: inline-flex;
      align-items: center;
      gap: 0.6rem;
      background: #fff;
      color: var(--clay-dark);
      font-weight: 700;
      font-size: 1rem;
      padding: 0.85rem 2.2rem;
      border-radius: 50px;
      text-decoration: none;
      transition: transform 0.2s, box-shadow 0.2s;
      letter-spacing: 0.03em;
    }

    .btn-etsy-big:hover { transform: translateY(-2px); box-shadow: 0 8px 20px rgba(0,0,0,0.15); }

    .etsy-icon {
      width: 22px;
      height: 22px;
    }

    /* CONTACT */
    #kontaktua {
      background: var(--cream);
      max-width: 680px;
      margin: 0 auto;
    }

    .contact-wrapper {
      max-width: 560px;
      margin: 0 auto;
    }

    .contact-wrapper .section-label,
    .contact-wrapper .section-title,
    .contact-wrapper .section-sub { text-align: center; }

    .contact-wrapper .section-sub { margin: 0 auto 2.5rem; }

    .form-notice {
      background: var(--clay-light);
      border-left: 3px solid var(--clay);
      border-radius: 4px;
      padding: 0.85rem 1.1rem;
      font-size: 0.88rem;
      color: var(--clay-dark);
      margin-bottom: 2rem;
    }

    /* Google Form iframe */
    .google-form-wrapper {
      border-radius: 12px;
      overflow: hidden;
      border: 1px solid rgba(107,124,63,0.2);
      background: #fff;
    }

    .google-form-wrapper iframe {
      width: 100%;
      height: 680px;
      border: none;
      display: block;
    }

    /* FOOTER */
    footer {
      background: var(--earth);
      color: rgba(255,255,255,0.6);
      text-align: center;
      padding: 2rem;
      font-size: 0.88rem;
    }

    footer a {
      color: var(--clay-light);
      text-decoration: none;
    }

    footer a:hover { color: #fff; }

    footer .footer-brand {
      font-family: var(--ink);
      font-size: 1.4rem;
      color: #fff;
      margin-bottom: 0.5rem;
      display: block;
    }

    /* ANIMATIONS */
    @keyframes fadeUp {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }

    /* LANG SWITCHER */
    .lang-switcher {
      display: flex;
      align-items: center;
      gap: 2px;
      background: var(--clay-light);
      border-radius: 50px;
      padding: 3px;
    }

    .lang-btn {
      background: transparent;
      border: none;
      cursor: pointer;
      font-family: var(--body);
      font-size: 0.72rem;
      font-weight: 700;
      letter-spacing: 0.04em;
      color: var(--clay-dark);
      padding: 4px 9px;
      border-radius: 50px;
      transition: background 0.2s, color 0.2s;
      line-height: 1;
    }

    .lang-btn.active {
      background: var(--clay);
      color: #fff;
    }

    .lang-btn:hover:not(.active) {
      background: rgba(107,124,63,0.15);
    }

    /* RESPONSIVE */
    @media (max-width: 600px) {
      .nav-links { gap: 0.6rem; }
      .nav-links a { font-size: 0.78rem; }
      .brand-name { font-size: 1.2rem; }
      .lang-switcher { display: none; }
    }
  </style>
</head>
<body>

  <!-- NAV -->
  <nav>
    <a href="#" class="nav-logo">
      <div class="logo-img">
        <!--
          LOGO: Reemplaza el contenido de .logo-img con:
          <img src="tu-logo.png" alt="Akibuz logo" />
          y elimina el span de abajo.
        -->
        <span class="logo-placeholder-text">A</span>
      </div>
      <span class="brand-name">Akibuz</span>
    </a>
    <ul class="nav-links">
      <li><a href="#produktuak">Lanak</a></li>
      <li><a href="#kontaktua">Kontaktua</a></li>
      <li>
        <a href="https://www.instagram.com/akibuz.ceramics/" target="_blank" rel="noopener" class="btn-instagram" title="Instagram">
          <svg width="18" height="18" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg" style="display:block;">
            <rect x="2" y="2" width="20" height="20" rx="5" stroke="currentColor" stroke-width="2"/>
            <circle cx="12" cy="12" r="4" stroke="currentColor" stroke-width="2"/>
            <circle cx="17.5" cy="6.5" r="1" fill="currentColor"/>
          </svg>
        </a>
      </li>
      <li><a href="https://akibuz.etsy.com" target="_blank" rel="noopener" class="btn-etsy">Etsy Denda ↗</a></li>
    </ul>
  </nav>

  <!-- HERO -->
  <section class="hero">
    <p class="hero-tag">✦ Eskuz egindako zeramika</p>
    <h1>Akibuz<br/>Ceramics</h1>
    <p>Buztinezko sorkuntza paregabeak zure egunerokotasunerako. Bakoitza bakarra.</p>
    <div class="hero-ctas">
      <a href="#produktuak" class="btn-primary">Katalogoa ikusi</a>
      <a href="#kontaktua" class="btn-outline">Kontaktua</a>
    </div>
    <div class="scroll-down"><span></span></div>
  </section>

  <!-- KATALOGOA -->
  <section id="produktuak">
    <div class="catalog-header">
      <p class="section-label">Katalogoa</p>
      <h2 class="section-title">Nire Lanak</h2>
      <p class="section-sub">Pieza bakoitza eskuz eta bihotzez egina. Enkargu pertsonalizatuak ere onartzen ditut.</p>
    </div>
    <div class="catalog-grid">
      <div class="product-card">
        <img src="akibuz1.png" alt="Zeramika 1" />
        <div class="card-body">
          <h3>Katilua</h3>
          <p>Eskuz torneatu eta glazuratu. Pieza bakarra.</p>
        </div>
      </div>
      <div class="product-card">
        <img src="akibuz2.png" alt="Zeramika 2" />
        <div class="card-body">
          <h3>Loreontzia</h3>
          <p>Diseinu organikoa zure etxerako.</p>
        </div>
      </div>
      <div class="product-card">
        <img src="akibuz3.png" alt="Zeramika 3" />
        <div class="card-body">
          <h3>Platera</h3>
          <p>Eguneroko erabilerarako pieza dotore eta iraunkorra.</p>
        </div>
      </div>
      <div class="product-card">
        <img src="akibuz4.png" alt="Zeramika 4" />
        <div class="card-body">
          <h3>Pieza 4</h3>
          <p>Zure irudia hemen.</p>
        </div>
      </div>
      <div class="product-card">
        <img src="akibuz5.png" alt="Zeramika 5" />
        <div class="card-body">
          <h3>Pieza 5</h3>
          <p>Zure irudia hemen.</p>
        </div>
      </div>
      <div class="product-card">
        <img src="akibuz6.png" alt="Zeramika 6" />
        <div class="card-body">
          <h3>Pieza 6</h3>
          <p>Zure irudia hemen.</p>
        </div>
      </div>
      <div class="product-card">
        <img src="akibuz7.png" alt="Zeramika 7" />
        <div class="card-body">
          <h3>Pieza 7</h3>
          <p>Zure irudia hemen.</p>
        </div>
      </div>
      <div class="product-card">
        <img src="akibuz8.png" alt="Zeramika 8" />
        <div class="card-body">
          <h3>Pieza 8</h3>
          <p>Zure irudia hemen.</p>
        </div>
      </div>
      <div class="product-card">
        <img src="akibuz9.png" alt="Zeramika 9" />
        <div class="card-body">
          <h3>Pieza 9</h3>
          <p>Zure irudia hemen.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- ETSY BANNER -->
  <div class="etsy-banner">
    <p>Gehiago aurkitu nahi duzu?<br/>Bisitatu nire Etsy denda!</p>
    <a href="https://akibuz.etsy.com" target="_blank" rel="noopener" class="btn-etsy-big">
      <svg class="etsy-icon" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
        <path d="M5 4C5 4 5.5 6 5.5 12C5.5 18 5 20 5 20H8.5C8.5 20 10 19 12.5 19C15 19 16 20 16 20V16.5C16 16.5 14.5 17 12.5 17C10.5 17 9.5 16.5 9.5 16.5V13H13.5V10H9.5V7.5C9.5 7.5 10.5 7 12.5 7C14.5 7 16 7.5 16 7.5V4C16 4 14.5 4.5 12 4.5C9.5 4.5 8 4 8 4H5Z" fill="#6b7c3f"/>
      </svg>
      Akibuz Etsy denda
    </a>
  </div>

  <!-- KONTAKTUA -->
  <section id="kontaktua" style="padding: 5rem 2rem;">
    <div class="contact-wrapper">
      <p class="section-label">Kontaktua</p>
      <h2 class="section-title">Idatzi iezadazu</h2>
      <p class="section-sub">Gustatu zaizu zerbait edo enkargu pertsonalizatu bat nahi duzu?</p>

      <!--
        ════════════════════════════════════════════════════════════
        GOOGLE FORMS INSTRUKZIOAK:
        1. Sortu zure Google Form hemen: https://forms.google.com
        2. Klik "Bidali" > "<>" (embed) ikonoan
        3. Kopiatu iframe src="..." URLa
        4. Ordezkatu beheko ZURE_FORM_ID_HEMEN zure IDarekin

        URLa honelakoa izango da:
        https://docs.google.com/forms/d/e/XXXXXXX/viewform?embedded=true
        ════════════════════════════════════════════════════════════
      -->

      <div class="form-notice">
        ⚙️ <strong>Oharra:</strong> Google Forms lotura jarri behar da. Beheko instrukzioak jarraitu.
      </div>

      <div class="google-form-wrapper">
        <iframe
          src="ZURE_GOOGLE_FORMS_URL_HEMEN"
          title="Kontaktu formularioa"
          frameborder="0"
          marginheight="0"
          marginwidth="0"
          loading="lazy"
        >
          Kargatzen...
        </iframe>
      </div>
    </div>
  </section>

  <!-- FOOTER -->
  <footer>
    <span class="footer-brand">Akibuz Ceramics</span>
    <p>
      <a href="https://akibuz.etsy.com" target="_blank" rel="noopener">Etsy denda</a>
      &nbsp;·&nbsp;
      <a href="https://www.instagram.com/akibuz.ceramics/" target="_blank" rel="noopener">Instagram</a>
    </p>
    <p style="margin-top: 0.75rem;">© 2026 Akibuz Ceramics. Eskubide guztiak erreserbatuta.</p>
  </footer>

</body>
</html>
